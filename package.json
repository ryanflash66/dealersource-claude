{
  "name": "dealersource",
  "version": "0.1.0",
  "private": true,
  "description": "Finds, verifies and ranks leaseable sites for a used-car dealership in Eastern NC. Offline-first, provider-abstracted pipeline plus static dashboard.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p dashboard/tsconfig.json --noEmit",
    "pipeline": "tsx src/cli.ts run",
    "pipeline:golden": "tsx src/cli.ts run --offline --fixtures fixtures/golden-v1 --out out/golden --run-date 2026-09-16",
    "sources:check": "tsx src/cli.ts sources check",
    "sources:discover": "tsx src/cli.ts sources discover",
    "dashboard:build": "node scripts/build-dashboard.mjs",
    "dashboard:dev": "node scripts/build-dashboard.mjs && node scripts/serve-dashboard.mjs",
    "dashboard:deploy": "vercel deploy --prod --yes",
    "db:local": "docker compose up -d",
    "db:migrate": "node scripts/apply-migrations.mjs"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/node": "^24.13.5",
    "@types/nodemailer": "^8.0.2",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "tsx": "^4.23.13",
    "typescript": "^5.9.3",
    "vitest": "^3.2.7"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.126.0",
    "imapflow": "^2.0.6",
    "nodemailer": "^10.0.10",
    "playwright": "^1.63.0",
    "yaml": "^2.9.1",
    "zod": "^3.25.76"
  }
}
